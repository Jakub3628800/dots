#!/bin/bash

if [ "$XDG_SESSION_TYPE" = "wayland" ]; then
	wdisplays
elif [ "$XDG_SESSION_TYPE" = "x11" ]; then
    autorandr --cycle
else
    echo "XDG_SESSION is not x11 or wayland"
fi
bash ~/.local/bin/keyboard
bash ~/.local/bin/restore_workspaces
